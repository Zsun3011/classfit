import React from "react";
import Header from "../../components/Header";

const AITimetable = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default AITimetable;